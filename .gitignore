# secrets — NEVER commit
.env

# python
.venv/
__pycache__/
*.pyc

# data — don't commit raw datasets
data/*.csv
data/*.parquet

# models — regenerate locally, don't commit trained binaries
models/*.joblib

# os
.DS_Store
