"""Pydantic output models for parsing LLM responses.

You'll build this out on WEDNESDAY (7/9). Left as a scaffold on purpose —
writing it yourself is the point of that day's practice.
"""

# from pydantic import BaseModel, Field
#
# class FeedbackLabel(BaseModel):
#     sentiment: str          # e.g. "positive" | "neutral" | "negative"
#     topic: str              # e.g. "performance", "ui", "content"
#     confidence: float       # 0.0 - 1.0
#
# TODO (Wed): finish 3 structured-output models for LLM JSON parsing.
