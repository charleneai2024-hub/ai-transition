"""Reusable LLM client wrapper (retry + cost logging).

You'll build this out on THURSDAY (7/10). The flagship project will import
from here, so keep the interface clean. Left as a scaffold on purpose.
"""

# from anthropic import Anthropic
#
# def call_llm(prompt: str, model: str = "claude-haiku-4-5", max_tokens: int = 512) -> str:
#     """Send one prompt, return text. Add retry + token/cost logging on Thursday."""
#     ...
#
# TODO (Thu): implement retry on errors + print token usage / estimated cost.
